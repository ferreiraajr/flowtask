services:
  # PHP Service
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel/app
    container_name: flowtask-app
    tty: true
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    working_dir: /var/www
    volumes:
      - ./:/var/www:delegated
      - ./docker_config_files/php/local.ini:/usr/local/etc/php/conf.d/local.ini:delegated
      - ~/.composer/docker-cache/:/root/.composer:cached
    networks:
      - flowtask-networks

  # Nginx Service
  webserver:
    image: nginx:alpine
    container_name: flowtask-webserver
    tty: true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./:/var/www:delegated
      - ./docker_config_files/nginx/conf.d/:/etc/nginx/conf.d/:delegated
    networks:
      - flowtask-networks
    depends_on:
      - postgres
      - app

  # Postgres Service
  postgres:
    container_name: flowtask-postgres
    image: postgres:latest
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres:delegated
    ports:
      - "5432:5432"
    networks:
      - flowtask-networks

  # PgAdmin
  pgadmin:
    container_name: flowtask-pgadmin
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@flowtask.br
      PGADMIN_DEFAULT_PASSWORD: admin
    volumes:
      - pgadmin:/root/.pgadmin
    ports:
      - "8080:80"
    networks:
      - flowtask-networks
    depends_on:
      - postgres

  # Composer
  composer:
    build:
      context: .
      dockerfile: Dockerfile-composer
    container_name: flowtask-composer
    volumes:
      - ./:/var/www/
    working_dir: /var/www/
    depends_on:
      - app
    networks:
      - flowtask-networks

  # NPM
  npm:
    image: node:20
    container_name: flowtask-npm
    volumes:
      - ./:/var/www/
    working_dir: /var/www/
    entrypoint: [ "npm" ]
    ports:
    - "5173:5173"
    networks:
      - flowtask-networks

  # Artisan
  artisan:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flowtask-artisan
    volumes:
      - ./:/var/www
    depends_on:
      - postgres
    working_dir: /var/www/
    entrypoint: [ "php", "/var/www/artisan" ]
    networks:
      - flowtask-networks

# Docker Networks
networks:
  flowtask-networks:
    driver: bridge

# Volumes
volumes:
  dbdata:
    driver: local
  postgres:
  pgadmin:
